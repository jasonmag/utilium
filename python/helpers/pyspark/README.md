Install pyspark local 

Ubuntu:

1. Create a virtual environment
python3 -m venv ~/pyspark-env

2. Activate the virtual environment
source ~/pyspark-env/bin/activate

3. Install PySpark inside the virtual environment
pip install pyspark

